<h1>{{title}}</h1>

<ul>
    {{#if author_list}}
        {{#each author_list}}
            <li>
                <a href={{this.url}}>{{this.name}}</a> ( {{this.lifespan_formatted}} )
            </li>
        {{/each}}
    {{else}}
        <li><p>There are no authors.</p></li>
    {{/if}}
</ul>