<h1>{{title}}</h1>

<ul>
    {{#if genre_list}}
        {{#each genre_list}}
            <li>
                <a href={{this.url}}>{{this.name}}</a>
            </li>
        {{/each}}
    {{else}}
        <li><p>There are no genres.</p></li>
    {{/if}}
</ul>